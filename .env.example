# Standalone med-referral app — TEST environment
MONGODB_URI=mongodb://localhost:27017/med_referral_test   # use a SEPARATE test DB, never CR production
PORT=5000
FIELD_ENCRYPTION_KEY=                                      # base64 32 bytes: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
STUB_CLINICIAN_ID=000000000000000000000001                # stub auth only — replace before real use
